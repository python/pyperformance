[project]
name = "pyperformance_bm_btree"
requires-python = ">=3.9"
dependencies = ["pyperf"]
urls = {repository = "https://github.com/python/pyperformance"}
dynamic = ["version"]

[tool.pyperformance]
name = "btree"
