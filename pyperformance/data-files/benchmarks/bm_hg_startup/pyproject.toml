[project]
name = "bm_hg_startup"
dependencies = [
    "mercurial",
]

# XXX This should be inherited from metabase.
dynamic = [
  "name",
  "version",
]

[tool.pyperformance]
# "name" is set automatically.
metabase = ".."
