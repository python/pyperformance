[project]
name = "pyperformance_bm_tomllib_loads"
requires-python = ">=3.11"
dependencies = ["pyperf"]
urls = {repository = "https://github.com/python/pyperformance"}
dynamic = ["version"]

[tool.pyperformance]
name = "tomllib_loads"
tags = "serialize"
