[project]
name = "bm_chameleon"
dependencies = [
    "Chameleon",
]

# XXX This should be inherited from metabase.
dynamic = [
  "name",
  "version",
]

[tool.pyperformance]
# "name" is set automatically.
metabase = ".."
