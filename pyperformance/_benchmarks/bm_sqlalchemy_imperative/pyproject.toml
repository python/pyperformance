[project]
name = "bm_sqlalchemy_imperative"
dependencies = [
    "SQLAlchemy",
]

# XXX This should be inherited from metabase.
dynamic = [
  "name",
  "version",
]

[tool.pyperformance]
# "name" is set automatically.
metabase = ".."
